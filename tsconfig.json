{
  // Este archivo es para la raíz del monorepo
  // La configuración principal está en functions/tsconfig.json
  "compilerOptions": {
    "module": "nodenext",
    "target": "esnext",
    "strict": true,
    "skipLibCheck": true
  }
}
