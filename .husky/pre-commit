#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Lint backend
echo "🔍 Linting backend..."
cd functions && pnpm lint

# Lint frontend
echo "🔍 Linting frontend..."
cd ../frontend-app && ng lint